using UnityEngine;

namespace ExcelHell.Prototype
{
    /// <summary>
    /// Compatibility shim for the retired target-local Outline tutorial guide.
    /// PrototypeGuidedOnboarding now owns top-most contextual highlight frames.
    /// </summary>
    public sealed class PrototypeTutorialVisualGuide : MonoBehaviour
    {
        // Intentionally no runtime bootstrap.
    }
}
